# AGENTS.md

## 프로젝트 개요

이 저장소는 NEIS 공개 API를 활용해 초중고 급식 메뉴를 조회하고 AI
에이전트로 분석하는 웹 애플리케이션을 단계적으로 구현하는 워크숍입니다.
현재는 워크숍 문서와 원본 API 자료가 중심이며, 프론트엔드와 백엔드는 이후
단계에서 추가될 수 있습니다.

## 일반 작업 지침

- 요청된 변경 범위 안에서만 작업하고 관련 없는 코드는 수정하지 않습니다.
- `AGENTS.md`보다 구체적인 디렉터리별 지침, 기존 코드 관례, 설정 파일,
  `CONTRIBUTING.md`를 우선합니다.
- 기존 구현과 의존성을 먼저 확인하고 재사용합니다.
- 명확한 필요가 없는 추상화, 프레임워크, 의존성, 설정을 추가하지 않습니다.
- 동작 변경은 작고 검토 가능한 단위로 구현하며 기존 동작을 보존합니다.

## Python 가이드라인

- Python 코드는 [PEP 8](https://peps.python.org/pep-0008/)을 기본 스타일
  가이드로 따르되, 프로젝트에 설정된 포매터와 린터 규칙을 우선합니다.
- 공개 함수와 복잡한 내부 경계에 명확한 타입 힌트를 사용하고 타입 검사를
  우회하는 광범위한 `Any`나 무분별한 형 변환을 피합니다.
- 예상 가능한 오류는 구체적인 예외로 처리하고, 예외를 숨기거나 성공처럼
  보이는 기본값으로 대체하지 않습니다.
- 의존성은 프로젝트가 채택한 패키지 및 잠금 파일을 통해서만 관리하고,
  표준 라이브러리나 기존 의존성으로 해결 가능한 패키지는 추가하지 않습니다.
- 모듈은 단일 책임을 유지하고 중복보다 명확한 함수와 데이터 구조를
  선호합니다.

## TypeScript 가이드라인

- TypeScript 코드는
  [Mozilla 코딩 스타일](https://firefox-source-docs.mozilla.org/code-quality/coding-style/)
  의 JavaScript 지침을 기본으로 참고하되, 프로젝트에 설정된 포매터, 린터 및
  TypeScript 설정을 우선합니다.
- 엄격한 타입을 유지하고 `any`, 불필요한 타입 단언, null 가능성의 무시를
  피합니다. 외부 입력은 사용 전에 검증하고 좁혀서 처리합니다.
- 비동기 오류를 누락하지 말고 사용자 또는 호출자가 대응할 수 있는 형태로
  전달합니다. 빈 `catch`나 오류를 숨기는 fallback을 사용하지 않습니다.
- 의존성은 기존 패키지 관리자와 잠금 파일로 관리하며, 동일한 기능의
  라이브러리를 중복 추가하지 않습니다.
- 기존 컴포넌트와 유틸리티를 재사용하고 과도한 추상화나 불필요한 상태를
  만들지 않습니다.

## 테스트 및 유효성 검사

- 변경된 동작에는 적절한 테스트를 추가하거나 기존 테스트를 수정합니다.
- 변경 범위에 맞는 테스트, 포맷팅, 린트, 타입 검사 및 빌드를 실행합니다.
- 실행 명령은 현재 저장소의 매니페스트, 스크립트, CI 또는 기여 문서에 실제로
  정의된 것만 사용합니다. 아직 구성되지 않은 도구나 명령을 임의로 도입하거나
  통과한 것으로 간주하지 않습니다.
- 검사할 수 없는 항목은 생략 사실과 이유를 명확히 기록합니다.

## 보안

- 비밀, 자격 증명, 토큰, 개인정보를 코드, 로그, 테스트 데이터 또는 커밋에
  포함하지 않습니다.
- `.env`, `.env.*`(예제 파일 제외), 개인 키(`*.pem`, `*.key`), 자격 증명 및
  토큰 파일은 커밋하지 않습니다. 공유할 환경 변수는 비밀 값이 없는
  `.env.example` 같은 예제 파일로 문서화합니다.
- 외부 입력을 신뢰하지 말고 검증하며, 최소 권한과 안전한 기본값을 사용합니다.
- 보안 취약점은 공개 이슈에 작성하지 않고 `SECURITY.md` 절차를 따릅니다.

## 문서화

- 사용자 동작, 공개 API, 설정 또는 개발 절차가 바뀌면 관련 문서를 함께
  갱신합니다.
- 주석은 코드만으로 의도가 드러나지 않는 경우에만 작성하고 구현을 그대로
  반복하지 않습니다.

## 주의사항 및 가드레일

- 생성된 파일, 잠금 파일, 마이그레이션 및 배포 설정을 수동으로 변경하기 전에
  해당 프로젝트의 생성·관리 방식을 확인합니다.
- 기존 사용자 변경을 덮어쓰거나 요청 없이 파괴적 Git 명령, 대규모 이름 변경,
  전면 포맷팅을 수행하지 않습니다.
- 불확실한 요구사항이 동작, 호환성 또는 보안에 영향을 주면 추측하지 말고
  확인합니다.

## Git 커밋 및 Pull Request

- 커밋은 하나의 논리적 변경에 집중하고 Conventional Commits 형식
  (`feat:`, `fix:`, `docs:`, `test:`, `refactor:`, `chore:`)을 따릅니다.
- 커밋 메시지와 PR에는 변경 내용과 이유를 명확히 설명합니다.
- PR은 `.github/PULL_REQUEST_TEMPLATE.md`를 사용해 관련 이슈, 변경 유형,
  검증 결과 및 체크리스트를 빠짐없이 작성합니다.
- PR을 제출하기 전에 자체 검토하고 관련 검사와 문서 변경 여부를 확인합니다.
